name: Add Issue to Project
on:
  issues:
    types: [opened]

jobs:
  sync:
    uses: Negative-Technolohies/paydeya/.github/workflows/sync-issue-to-project.yml@main
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_ACCESS_TOKEN }}
