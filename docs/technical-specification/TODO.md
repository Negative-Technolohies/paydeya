# TODO: Изменения модели данных

Документ описывает все изменения БД и хранилищ, которые необходимо внести на основании технического задания по сценариям (разделы 3–14).

---

## 1. Таблица `users` — правки

| Действие | Поле             | Тип                                                                       | Комментарий                                                                                                | Источник |
| -------- | ---------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------- |
| Добавить | `full_name`      | String(150), NOT NULL                                                     | ФИО пользователя                                                                                           | Раздел 3 |
| Добавить | `status`         | Enum: `unconfirmed`, `active`, `blocked`; NOT NULL; default `unconfirmed` | Статус учётной записи: неподтверждённая / активная / заблокированная                                       | Раздел 3 |
| Добавить | `email_verified` | Boolean, NOT NULL, default false                                          | Подтверждён ли email                                                                                       | Раздел 3 |
| Добавить | `phone_verified` | Boolean, NOT NULL, default false                                          | Подтверждён ли телефон                                                                                     | Раздел 3 |
| Добавить | `is_banned`      | Boolean, NOT NULL, default false                                          | Флаг полной блокировки; при `true` сервер не отвечает на запросы пользователя                               | Разделы 17, 19 |
| Добавить | Уникальность     | Partial Unique Index                                                      | `email` и `phone` уникальны среди непустых значений (`WHERE email IS NOT NULL`, `WHERE phone IS NOT NULL`) | Раздел 3 |

> Примечание: `users.is_banned` и `users.status = 'blocked'` (раздел 3) описывают одно и то же состояние. Согласовать:** оставить одно из двух (или два поля, синхронизируемые на стороне приложения)** — открытый вопрос.

---

## 2. Таблица `student_profiles` — правки

| Действие | Поле                      | Тип                                              | Комментарий                                                                                 | Источник     |
| -------- | ------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------- | ------------ |
| Добавить | `education_level_id`      | BigInteger, FK → `education_levels.id`, NOT NULL | Уровень образования ученика (обязательное поле)                                             | Раздел 4     |
| Добавить | `city_id`                 | BigInteger, FK → `cities.id`, NULL               | Город проживания. Необязателен в профиле; обязателен в офлайн-анкете                        | Раздел 4     |
| Добавить | `about`                   | String(500), NULL                                | «О себе»                                                                                    | Раздел 4     |
| Добавить | `avatar_key`              | String, NULL                                     | Ссылка на файл аватара в хранилище                                                          | Раздел 4     |
| Добавить | `is_searching`            | Boolean, NOT NULL, default false                 | Глобальный переключатель «Ищу репетитора»                                                   | Разделы 4, 6 |
| Добавить | `updated_at`              | datetime, NOT NULL                               | Время последнего изменения профиля                                                          | Раздел 4     |
| Удалить  | `verified`                | —                                                | Состояние подтверждения живёт на `users` (раздел 3)                                         | Раздел 3     |
| Удалить  | `active_questionnaire_id` | —                                                | Несовместим с правилом «одна активная анкета на предмет»; источник правды — `status` анкеты | Раздел 5     |
| Заменить | `city` (String)           | → `city_id` (FK → `cities.id`, NULL)             | Строка заменяется на FK к справочнику                                                       | Раздел 4     |

---

## 3. Таблица `questionnaires` — правки

| Действие          | Поле                     | Тип                                                            | Комментарий                                                       | Источник |
| ----------------- | ------------------------ | -------------------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| Добавить          | `subject_id`             | BigInteger, FK → `subjects.id`, NOT NULL                       | Предмет анкеты                                                    | Раздел 5 |
| Добавить          | `format`                 | Enum: `online`, `offline`, `any`; NOT NULL                     | Формат занятий                                                    | Раздел 5 |
| Добавить          | `city_id`                | BigInteger, FK → `cities.id`, NULL                             | Город (обязателен при format = offline; контроль приложения)      | Раздел 5 |
| Добавить          | `budget_min`             | Numeric, NULL                                                  | Нижняя граница бюджета за занятие (60 мин), ₽                     | Раздел 5 |
| Добавить          | `budget_max`             | Numeric, NULL                                                  | Верхняя граница бюджета за занятие (60 мин), ₽                    | Раздел 5 |
| Добавить          | `budget_negotiable`      | Boolean, NOT NULL, default false                               | Признак «договорная цена»                                         | Раздел 5 |
| Добавить          | `schedule`               | String(300), NULL                                              | Удобное время                                                     | Раздел 5 |
| Добавить          | `description`            | String(1000), NULL                                             | Комментарий к анкете                                              | Раздел 5 |
| Добавить          | `status`                 | Enum: `draft`, `active`, `archived`; NOT NULL; default `draft` | Трёхстатусная модель вместо Boolean `search_status`               | Раздел 5 |
| Добавить          | `updated_at`             | datetime, NULL                                                 | Время последнего редактирования                                   | Раздел 5 |
| Добавить          | `activated_at`           | datetime, NULL                                                 | Время последней активации                                         | Раздел 5 |
| Изменить          | `title`                  | → nullable                                                     | Сейчас NOT NULL; сделать необязательным (автогенерация на фронте) | Раздел 5 |
| Заменить          | `goals` (Text)           | → `goal` (String(300), NULL)                                   | Необязательное поле + лимит длины                                 | Раздел 5 |
| Заменить          | `budget` (Numeric, NULL) | → `budget_min` / `budget_max` + `budget_negotiable`            | Диапазон вместо одного значения                                   | Раздел 5 |
| Удалить           | `search_status`          | —                                                              | Разделён на `status` анкеты + `is_searching` профиля              | Раздел 5 |
| Уникальный индекс | —                        | `(student_id, subject_id) WHERE status = 'active'`             | Гарантия: одна активная анкета на предмет на уровне БД            | Раздел 5 |

---

## 4. Таблица `conversations` — правки

| Действие          | Поле              | Тип                                   | Комментарий                                                                      | Источник     |
| ----------------- | ----------------- | ------------------------------------- | -------------------------------------------------------------------------------- | ------------ |
| Добавить          | `initiator_id`    | BigInteger, FK → `users.id`, NOT NULL | Кто начал диалог (отправил первое сообщение). Для лимита сообщений               | Раздел 10    |
| Добавить          | `student_user_id` | BigInteger, FK → `users.id`, NOT NULL | ID ученика из таблицы `users` (для уникального индекса на пару пользователей)    | Разделы 8, 9 |
| Добавить          | `tutor_user_id`   | BigInteger, FK → `users.id`, NOT NULL | ID репетитора из таблицы `users` (для уникального индекса на пару пользователей) | Разделы 8, 9 |
| Уникальный индекс | —                 | `(student_user_id, tutor_user_id)`    | Один диалог на пару ученик-репетитор на уровне БД                                | Разделы 8, 9 |

> Примечание: существующие FK `student_id` → `student_profiles.id` и `tutor_id` → `tutor_profiles.id` сохраняются. Поля `student_user_id` и `tutor_user_id` добавляются для построения уникального ограничения на уровне `users`.

---

## 5. Таблица `reviews` — правки

| Действие          | Поле           | Тип                                               | Комментарий                                                   | Источник  |
| ----------------- | -------------- | ------------------------------------------------- | ------------------------------------------------------------- | --------- |
| Добавить          | `agreement_id` | BigInteger, FK → `agreement_surveys.id`, NOT NULL | На основании какой подтверждённой договорённости создан отзыв | Раздел 14 |
| Добавить          | `updated_at`   | datetime, NULL                                    | Время последнего редактирования                               | Раздел 14 |
| Добавить          | `deleted_at`   | datetime, NULL                                    | Мягкое удаление                                               | Раздел 14 |
| Изменить          | `text`         | → NOT NULL                                        | Текст отзыва обязателен                                       | Раздел 14 |
| Уникальный индекс | —              | `(author_id, target_id) WHERE deleted_at IS NULL` | Один активный отзыв на пару ученик-репетитор                  | Раздел 14 |
| Индекс            | —              | `(target_id, deleted_at)`                         | Быстрый пересчёт рейтинга                                     | Раздел 14 |

### Агрегаты в `tutor_profiles` (обязательные поля)

| Поле            | Тип                          | Комментарий                               | Источник      |
| --------------- | ---------------------------- | ----------------------------------------- | ------------- |
| `avg_rating`    | Numeric(2,1), NULL           | Средний рейтинг по опубликованным отзывам | Разделы 7, 14 |
| `reviews_count` | Integer, NOT NULL, default 0 | Количество опубликованных отзывов         | Разделы 7, 14 |

---

## 6. Таблица `tutor_profiles` — правки

| Действие | Поле                  | Тип                                                  | Комментарий                                                                          | Источник      |
| -------- | --------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------- |
| Добавить | `avg_rating`          | Numeric(2,1), NULL                                   | Средний рейтинг (денормализованный агрегат, пересчёт при публикации/удалении отзыва) | Разделы 7, 14 |
| Добавить | `reviews_count`       | Integer, NOT NULL, default 0                         | Количество опубликованных отзывов                                                    | Разделы 7, 14 |
| Добавить | `avatar_key`          | String, NULL                                         | Ссылка на файл аватара в хранилище                                                   | Раздел 14     |
| Добавить | `verification_status` | Enum / String (точные значения — сценарий модерации) | Статус подтверждения квалификации. Фильтр каталога (7.3) читает его                  | Раздел 7      |

> Примечание: сценарий 17 вводит отдельную таблицу `tutor_verifications` (см. раздел 7 ниже) как источник отметок верификации («Личность подтверждена», «Образование подтверждено», «Сертификат подтверждён»). Согласовать: хранить `verification_status` на профиле (денормализация) или вычислять по `tutor_verifications` — открытый вопрос.

---

## 7. Новые таблицы

### `education_levels`

| Поле | Тип                      | Комментарий                                                   |
| ---- | ------------------------ | ------------------------------------------------------------- |
| id   | BigInteger, PK           |                                                               |
| name | String, NOT NULL, UNIQUE | Начальная школа, средняя школа, старшая школа, студент/высшее |

Заполнение — миграцией. Названия финально уточняются.

### `cities`

| Поле     | Тип              | Комментарий     |
| -------- | ---------------- | --------------- |
| id       | BigInteger, PK   |                 |
| name     | String, NOT NULL | Название города |
| timezone | String, NOT NULL | Часовой пояс    |

### `agreement_surveys`

| Поле            | Тип                                                               | Комментарий                     |
| --------------- | ----------------------------------------------------------------- | ------------------------------- |
| id              | BigInteger, PK                                                    |                                 |
| conversation_id | BigInteger, FK → `conversations.id`, NOT NULL, UNIQUE             | Один активный опрос на диалог   |
| status          | Enum: `pending`, `confirmed`, `rejected`, `unconfirmed`; NOT NULL | Статус опроса                   |
| initiated_at    | datetime, NOT NULL                                                | Когда создан опрос              |
| resolved_at     | datetime, NULL                                                    | Когда определён итоговый статус |

Частичный уникальный индекс: `(conversation_id) WHERE status = 'pending'`.

### `agreement_answers`

| Поле         | Тип                                               | Комментарий                                   |
| ------------ | ------------------------------------------------- | --------------------------------------------- |
| id           | BigInteger, PK                                    |                                               |
| survey_id    | BigInteger, FK → `agreement_surveys.id`, NOT NULL | К какому опросу                               |
| user_id      | BigInteger, FK → `users.id`, NOT NULL             | Кто отвечает                                  |
| notified_at  | datetime, NULL                                    | Когда отправлено уведомление                  |
| read_at      | datetime, NULL                                    | Когда прочитано (начало таймера)              |
| response     | Boolean, NULL                                     | true = да, false = нет, null = ещё не ответил |
| responded_at | datetime, NULL                                    | Время ответа                                  |

Уникальный индекс: `(survey_id, user_id)`.

### `complaints`

| Поле              | Тип                                                                           | Комментарий                                                               |
| ----------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| id                | BigInteger, PK                                                                |                                                                           |
| complainant_id    | BigInteger, FK → `users.id`, NOT NULL                                         | Кто подал жалобу                                                          |
| respondent_id     | BigInteger, FK → `users.id`, NOT NULL                                         | На кого подана                                                            |
| reason            | Enum: `spam`, `insult`, `fraud`, `other`; NOT NULL                            | Причина                                                                   |
| description       | String(1000), NULL                                                            | Описание при `other`                                                      |
| context_type      | String, NULL                                                                  | Тип контекста (`profile`, `questionnaire`, `review`, `video`, `material`) |
| context_id        | BigInteger, NULL                                                              | ID объекта контекста                                                      |
| status            | Enum: `new`, `under_review`, `resolved`, `dismissed`; NOT NULL; default `new` | Статус рассмотрения                                                       |
| created_at        | datetime, NOT NULL                                                            | Время подачи                                                              |
| resolved_at       | datetime, NULL                                                                | Время решения                                                             |
| resolved_by       | BigInteger, FK → `users.id`, NULL                                             | Модератор, принявший решение                                              |
| decision          | String, NULL                                                                  | `confirmed` / `not_confirmed`                                             |
| moderator_comment | String(1000), NULL                                                            | Комментарий модератора                                                    |

Индексы: `(complainant_id, respondent_id)` — проверка дубликатов; `status` — очередь модератора.

### `review_replies`

| Поле       | Тип                                     | Комментарий                 |
| ---------- | --------------------------------------- | --------------------------- |
| id         | BigInteger, PK                          |                             |
| review_id  | BigInteger, FK → `reviews.id`, NOT NULL | К какому отзыву ответ       |
| tutor_id   | BigInteger, FK → `users.id`, NOT NULL   | Репетитор, написавший ответ |
| text       | String(1000), NOT NULL                  | Текст ответа                |
| created_at | datetime, NOT NULL                      |                             |
| updated_at | datetime, NULL                          |                             |
| deleted_at | datetime, NULL                          | Мягкое удаление             |

> Таблица вводится как предложение: детали ответа на отзыв ( количество ответов, редактирование) — открытый вопрос №55.

### `verification_documents`

Загружаемые преподавателем документы для проверки модератором (сценарий 17).

| Поле         | Тип                                              | Комментарий                                                                                       |
| ------------ | ------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| id           | BigInteger, PK                                   |                                                                                                   |
| tutor_id     | BigInteger, FK → `tutor_profiles.id`, NOT NULL   | Преподаватель                                                                                     |
| type         | Enum: `passport`, `diploma`, `certificate`; NOT NULL | Тип документа                                                                                     |
| title        | String(200), NULL                                | Для сертификата — название                                                                        |
| file_keys    | JSONB, NULL                                      | Ключи файлов в объектном хранилище (или отдельная таблица)                                        |
| status       | Enum: `pending`, `approved`, `rejected`, `cancelled`; NOT NULL; default `pending` | Статус проверки                  |
| reviewer_id  | BigInteger, FK → `users.id`, NULL                | Модератор, принявший решение                                                                      |
| comment      | String(1000), NULL                               | Комментарий / причина отклонения                                                                  |
| created_at   | datetime, NOT NULL                               | Время загрузки                                                                                    |
| reviewed_at  | datetime, NULL                                   | Время решения                                                                                     |
| expires_at   | datetime, NULL                                   | Срок хранения (`reviewed_at + 30 дней`); по истечении — файлы и запись удаляются. Может быть продлён администратором |

Индекс: `(tutor_id, type, status)` — быстрый поиск активных документов.

### `tutor_verifications`

Подтверждённые верификации преподавателя (сценарий 17). Единый источник отметок «Личность подтверждена» / «Образование подтверждено» / «Сертификат подтверждён».

| Поле         | Тип                                                     | Комментарий                             |
| ------------ | ------------------------------------------------------- | --------------------------------------- |
| id           | BigInteger, PK                                          |                                         |
| tutor_id     | BigInteger, FK → `tutor_profiles.id`, NOT NULL          | Преподаватель                           |
| type         | Enum: `passport`, `diploma`, `certificate`; NOT NULL    | Тип верификации                         |
| title        | String(200), NULL                                       | Для сертификата — название              |
| verified_at  | datetime, NOT NULL                                      | Когда подтверждено                      |
| document_id  | BigInteger, FK → `verification_documents.id`, NULL      | Связь с последним одобренным документом |
| is_active    | Boolean, NOT NULL, default true                         | Активна ли (можно отозвать при reject)  |

Индекс: `(tutor_id, type)`.

### `external_review_requests`

Запрос преподавателя на импорт отзывов с внешних платформ (сценарий 19).

| Поле           | Тип                                                  | Комментарий                                                                                  |
| -------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| id             | BigInteger, PK                                       |                                                                                              |
| tutor_id       | BigInteger, FK → `tutor_profiles.id`, NOT NULL       | Преподаватель                                                                                |
| platform_name  | String, NOT NULL                                     | Название внешней платформы (например, Profi.ru)                                              |
| url            | String, NOT NULL                                     | Ссылка на публичную страницу с отзывами                                                      |
| declared_count | Integer, NOT NULL                                    | Заявленное количество отзывов                                                                |
| status         | Enum: `pending`, `approved`, `rejected`, `processing`, `completed`, `failed`, `cancelled`; NOT NULL; default `pending` | Статус обработки |
| moderator_id   | BigInteger, FK → `users.id`, NULL                    | Модератор, обработавший запрос                                                                |
| comment        | String(1000), NULL                                   | Комментарий (обязателен при `reject`)                                                         |
| created_at     | datetime, NOT NULL                                   | Время подачи                                                                                 |
| processed_at   | datetime, NULL                                       | Время обработки                                                                              |

Индекс: `(tutor_id, status)`.

### `external_reviews`

Импортированные с внешних платформ отзывы (сценарий 19). Учитываются в общем рейтинге преподавателя наравне с внутренними.

| Поле          | Тип                                                          | Комментарий                                     |
| ------------- | ------------------------------------------------------------ | ----------------------------------------------- |
| id            | BigInteger, PK                                               |                                                 |
| tutor_id      | BigInteger, FK → `tutor_profiles.id`, NOT NULL               | Преподаватель                                   |
| request_id    | BigInteger, FK → `external_review_requests.id`, NOT NULL     | Из какого запроса импортирован                  |
| platform_name | String, NOT NULL                                             | Источник (внешняя платформа)                    |
| author_name   | String, NULL                                                 | Имя автора на внешней платформе (если доступно) |
| rating        | Integer, NOT NULL                                            | Оценка 1–5                                      |
| text          | String(2000), NOT NULL                                       | Текст отзыва                                    |
| source_url    | String, NULL                                                 | Ссылка на оригинальный отзыв (если доступна)    |
| created_at    | datetime, NOT NULL                                           | Время импорта                                   |

Индекс: `(tutor_id, request_id)`.

### Новые права (таблица `permissions`, сиды)

| Право                        | Комментарий                    | Источник  |
| ---------------------------- | ------------------------------ | --------- |
| `verify_tutor_documents`     | Проверка документов/верификация| Раздел 17 |
| `import_external_reviews`    | Импорт внешних отзывов         | Раздел 19 |

---

## 8. Redis (не PostgreSQL)

### `contact_confirmations`

Хранение подтверждений email/телефона при регистрации.

- **Хранилище:** Redis (не таблица PostgreSQL).
- **Назначение:** токены/коды подтверждения, попытки, TTL.
- **Спецификация:** формат ключей, структура значений, политика TTL — **описать отдельно при реализации**.

Ключевые требования (из раздела 3):

- Одно активное подтверждение на пару (user, contact_type).
- Хранится хеш кода/токена, а не значение.
- TTL: email-ссылка — 1 час, SMS-код — 10 минут.
- Лимит попыток ввода: не более 5 на одно подтверждение.
- Лимит повторной отправки: не чаще раза в 60 сек, не более 5 в час и 10 в сутки на учётную запись.
- Выпуск нового подтверждения аннулирует предыдущее того же типа.

---

## Сводка: количество изменений

| Категория                   | Количество                                                                                                                          |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Таблицы с правками          | 5 (`users`, `student_profiles`, `questionnaires`, `conversations`, `reviews`)                                                       |
| Таблица с агрегатами        | 1 (`tutor_profiles`)                                                                                                                |
| Новые таблицы PostgreSQL    | 9 (`education_levels`, `cities`, `agreement_surveys`, `agreement_answers`, `complaints`, `verification_documents`, `tutor_verifications`, `external_review_requests`, `external_reviews`) |
| Новая таблица (предложение) | 1 (`review_replies`)                                                                                                                |
| Новые права                 | 2 (`verify_tutor_documents`, `import_external_reviews`)                                                                             |
| Redis-хранилища             | 1 (`contact_confirmations` — спецификация в будущем)                                                                                |

---

## 9. Открытые вопросы, оставшиеся при написании ТЗ

### Открытые вопросы из сценариев

| №  | Вопрос                                                                                                  | Раздел | Текущее состояние / предложение                                                                                                   | Кто решает                  |
| -- | ------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| 15 | Состав первичной подборки репетиторов при активации анкеты (5.4, шаг 10)                                | 5, 7   | Состав — как в 7.6 п. 3 (матчинг по анкете); подача — во вкладке каталога с активным пресетом анкеты (см. №21)                    | владелец продукта           |
| 21 | Поведение каталога при нескольких активных анкетах ученика                                              | 7      | Обсуждение: A (чипы-пресеты анкет), B (вкладки «Подборка»/«Каталог»), C (умная лента). Рекомендация: v1 = A. До решения — рабочее поведение 7.4, шаг 2 | владелец продукта           |
| 23 | Состав карточки репетитора в каталоге                                                                   | 7      | Черновик — 7.6 п. 2; ляжет в матрицу видимости                                                                                    | владелец продукта           |
| 24 | Сортировка по умолчанию; позиция репетиторов без отзывов в выдаче                                       | 7      | Предложение: рейтинг ↓, без отзывов — в конец; сортировки: цена, новые                                                             | на согласовании             |
| 25 | Каталог для неавторизованных пользователей (SEO, маркетинг)                                             | 7      | v1 — только авторизованным                                                                                                        | на согласовании (маркетинг) |
| 29 | Проверять ли сервером при отклике соответствие репетитора параметрам анкеты                             | 8      | Сейчас сервер не перепроверяет (достаточно видимости анкеты в выдаче); возможно добавить мягкую проверку для защиты от ошибок UI  | владелец продукта           |
| 55 | Ответ репетитора на отзыв: сколько ответов, можно ли редактировать/удалять                              | 14     | Один ответ на отзыв, редактируемый, с мягким удалением — требует уточнения                                                        | владелец продукта           |
| 70 | Что происходит с материалами и отзывами, если профиль репетитора скрыт или забанен                       | 16     | Требуются отдельные механизмы; в v1 не решено                                                                                     | владелец продукта           |
| 72 | При отклонении документа: сбрасывать ли существующую верификацию по этому типу                          | 17     | По умолчанию не сбрасывать; модератор может отдельно указать «отозвать верификацию»                                               | владелец продукта           |
| 73 | Продление срока хранения документа (для разбирательств)                                                 | 17     | Администратор может продлить вручную                                                                                              | владелец продукта           |
| 91 | Какие платформы поддерживать в парсере внешних отзывов                                                   | 19     | Начать с Profi.ru, затем расширять                                                                                                | владелец продукта           |

### Вопросы, помеченные «закрытыми», но без финального решения

| №  | Вопрос                                                            | Раздел | Состояние |
| -- | ----------------------------------------------------------------- | ------ | --------- |
| 78 | Нужно ли разграничивать доступ к документам между модераторами?   | 17     | В таблице «Закрытые» указано «пока не решено, обсудим» — фактически открыто |

### Открытые вопросы, обнаруженные при составлении TODO (не из ТЗ)

| Вопрос                                                                                                                            | Где            | Состояние                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------- |
| `users.is_banned` (разделы 17, 19) ↔ `users.status = 'blocked'` (раздел 3) — описывают одно и то же состояние                      | TODO, раздел 1 | Оставить одно из двух, либо два поля синхронизируемые приложением             |
| `tutor_profiles.verification_status` (раздел 7) ↔ таблица `tutor_verifications` (раздел 17) — источник отметок верификации        | TODO, раздел 6 | Хранить денормализованный статус на профиле или вычислять по `tutor_verifications` |
| `contact_confirmations` — формат ключей, структура значений, политика TTL в Redis                                                | TODO, раздел 8 | Специфицировать отдельно при реализации                                     |
| Синхронизация формулировок разделов 7, 8, 9 с правилом из сценария 16: «опубликованный профиль» = `users.is_public = true` AND `tutor_profiles.is_active = true` (поля из сценария 16) | сценарий 16    | Внести правки в разделы 7, 8, 9                                       |
